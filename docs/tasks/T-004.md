# T-004: 퀀트 수집기: 1시간 주기 GitHub Actions 및 모바일 수동 실행 안정화

- **상태**: done (완료)
- **우선순위**: P1
- **카테고리**: quant_ops
- **단계**: 2. 퀀트 데이터 자동 수집
- **담당**: Gemini (Antigravity)
- **작업 완료일**: 2026-09-17

---

## 📋 요약
1시간 주기 크론 및 모바일 브라우저 `workflow_dispatch`를 통한 무중단 시세 수집 및 멱등적(idempotent) 파일 저장 파이프라인을 완성했습니다.
주문/매수/계좌 연동 로직은 0%이며, 순수 공개 시장 데이터(야후 60분봉 + 네이버 보조시세)만 수집합니다.

---

## 🎯 완료 조건 (DoD)
- [x] 실제 주문/매수/매도 API 작성 금지 불변식 준수
- [x] 외부 API 호출 시 타임아웃(10초) 및 지수 백오프(최대 3회) 재시도 로직 적용
- [x] 날짜별 폴더 구조(`data/{YYYYMMDD}/snapshot_{HH}00.json`) 및 일별 로그 멱등 저장 구현
- [x] 루트 `.github/workflows/collector.yml` 워크플로 생성 (스마트폰 모바일 웹 `workflow_dispatch` 지원)
- [x] 로컬 1회 실행 검증 (6개 종목 정상 수집 100% 성공)
- [x] `python tools/ai/quant_guard.py` 정적 안전 검사 통과 (32개 파일)
- [x] GitHub 원격 레포(`https://github.com/leobox/YNYM.git`) origin 등록

---

## 📝 수정 및 생성 파일
- `quant-collector/collector.py`: 수집 및 멱등 저장 스크립트
- `.github/workflows/collector.yml`: GitHub Actions 자동화 워크플로
- `quant-collector/data/20260917/snapshot_1300.json`: 로컬 검증 수집 데이터

---

## 🔒 안전성 및 멱등성 검증 결과
- `quant_guard.py`: 32개 소스 파일 정적 분석 통과 (금지된 주문 키워드 없음)
- 네트워크 단절 시 최대 3회 지수 백오프(1초 -> 2초 -> 4초)
- 동일 시간대에 여러 번 실행되어도 동일 시각 스냅샷 파일 1개로 덮어쓰여 데이터 무결성 유지

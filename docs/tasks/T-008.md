# T-008: Phase1-3: 패턴 엔진 통합 테스트 + Colab 배포 + 데이터 재현

- **상태**: done (최신 상태는 백로그 CLI 기준)
- **우선순위**: P0
- **카테고리**: quant_research
- **단계**: 1. 퀀트 알고리즘 연구
- **예상 소요**: 30분
- **작업 위치**: `quant-research/`
- **선행 작업**: 없음

---

## 📋 요약
T-007에서 이전한 패턴·체결 모듈을 오프라인 통합 검증하고, 독립 실행 Colab
Python을 한 코드 셀 노트북으로 배포하며, 이전 결과 데이터의 바이트 단위 재현성을 확인한다.

---

## 🎯 완료 조건 (DoD)
- [x] 패턴·돌파·후보·가상 체결 통합 및 prefix 불변성 검증
- [x] 독립 실행 Python과 출력 없는 단일 셀 Colab 노트북 동기화
- [x] 실행별 네 CSV와 manifest 원자적 저장
- [x] 이전 데이터 구조 검증 및 원본/대상 SHA256 트리 대조
- [x] 정적 검사·컴파일·문서 최신화

---

## 📝 작업 기록
- 2026-09-17: 작업 생성
- 2026-09-17, Codex: 백로그 상태를 `in_progress`로 재확인하고 T-007 결과를 인수함.
- 기존 테스트 42개는 통과했으나 `tools/sync_check.py`가 함수 3개를 형식 차이로
  오판했고, `tools/verify_data.py`가 소문자 열 이름으로 원본 DataFrame을 조회하여
  215개 CSV를 모두 실패 처리하는 결함을 발견함.
- 동기화 검사는 배포용 타입 힌트·docstring 차이를 제외하고 실행 AST를 비교하도록
  수정함. `pattern_colab.py`와 정확히 같은 한 코드 셀을 가진
  `notebooks/pattern_top5.ipynb`를 생성하는 `tools/build_notebook.py`를 추가함.
- 스캐너 모듈과 Colab 모두 무후보인 경우에도 universe/scores/top5/watch 네 CSV를
  원자적으로 저장하고, 행 수·SHA256·설정·공급원·코드 상태가 있는 manifest를 기록함.
- `tools/verify_data.py`를 경로 독립적으로 수정하고 timestamp, OHLCV, 중복, 순서,
  음수 거래량, 시간대를 검사하도록 강화함.
- `tools/gen_inventory.py`는 중첩 파일까지 포함하고 원본/대상의 상대경로·크기·SHA256을
  대조함. 선택된 6개 트리 497개 파일, 20,969,386바이트가 원본과 모두 동일함.
- 빈 후보 기록, manifest, 독립 Colab import, Python/노트북 동기화, 이전 데이터
  무결성, 미완료봉과 15시 표시용 봉을 검증하는 회귀 테스트를 추가함.

## 검증 결과

- `python -m pytest tests -v --tb=short`: 47 passed.
- `python tools/sync_check.py`: 핵심 함수 3개 및 노트북 코드 셀 일치.
- 원본 `test_hourly_pattern.bars()` 175개 봉에서 독립 Colab 점수와 기존
  `strategy.hourly_pattern`, 유지 신호와 `backtest_clear_trigger.features` 일치.
- `python tools/verify_data.py`: 215개 파일, 75,647봉, 2026-06-22 09:00~
  2026-09-14 14:00 KST; 구조 이상 없음.
- `python tools/gen_inventory.py`: 497개 파일의 원본/대상 트리 일치.
- `python -m compileall -q scanner research tools notebooks`: 통과.
- `python ..\\tools\\ai\\quant_guard.py`: 32개 소스 정적 검사 통과.
- `node ..\\tools\\backlog.mjs check`: 8개 작업 무결성 통과.
- 주문 API 호출, `shift(-1)`, import된 데이터의 자격증명형 파일명 없음.

## 남은 한계

- 네트워크를 사용하지 않는 검증만 수행했다. 현재 Yahoo/네이버 응답과 실제 Google
  Colab 런타임 실행은 미검증이다.
- 이전 데이터는 현재 고정 종목군이며 상장폐지 누락·선택/생존편향이 있다.
  15:00~15:30 구간과 공식 종가가 없고 공급원 간 수정주가·시장 기준도 미확인이다.
- 이 결과는 코드 재현 검증이며 안정적인 +10% 성과나 미래 확률의 검증이 아니다.
- 저장소가 최초 커밋 전이라 Git revision은 manifest에서 `null`, 작업 상태는 dirty로
  기록될 수 있다. 커밋·푸시·외부 배포는 수행하지 않았다.

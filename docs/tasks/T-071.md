# T-071: quant-collector: GitHub Actions 단일 워크플로 재등록(5분 리프레시) 및 메인 README 가독성 개편

- **상태**: done (완료)
- **우선순위**: P1
- **카테고리**: quant_ops
- **단계**: 2. 퀀트 데이터 자동 수집
- **예상 소요**: 20분
- **작업 위치**: `.github/workflows/collector.yml`, `quant-collector/collector.py`, `README.md`, `quant-collector/README.md`
- **선행 작업**: T-069

---

## 📋 요약
1. **GitHub Actions 워크플로 전면 단일화 재등록**:
   - 기존 `vcp-scanner.yml` 완전 삭제.
   - `collector.yml`을 단일 초경량 워크플로(`Quant 5m Auto Refresh Collector`)로 재작성.
   - `cron: '*/5 * * * *'` 설정으로 24시간 언제든 5분마다 상시 자동 실행 및 대시보드 리프레시 지원.
   - 불필요한 multi-job 의존성을 제거하고 단일 job으로 통합하여 1회 실행 시간 최소화.
2. **운영용 메인 README 및 모바일 대시보드 가독성 대폭 개편**:
   - 상단 요약 바를 카드형 배지(`⏱️ 실시간 갱신 | 📊 감시 유니버스 | ⚡ 조기/확정 돌파 | 🎯 활성 추적`)로 정돈.
   - 긴급 매도·청산 권고 신호를 구조화된 테이블(`구분`, `종목(코드)`, `현재가(수익률)`, `권고 사유`)로 리팩토링하여 MTS 대응 편의성 극대화.
   - Top 5 신규 진입 후보 표에 `돌파모드`(`⚡ 조기(10m/30m)` vs `🟢 확정(60m)`) 컬럼을 반영하여 한눈에 타점 식별 가능.

---

## 🎯 완료 조건 (DoD)
- [x] 기존 `vcp-scanner.yml` 삭제 및 `collector.yml` 단일 5분 상시 워크플로 재등록
- [x] 메인 `README.md` 및 `collector.py` 대시보드 가독성(테이블, 배지, 돌파모드) 개편
- [x] 실제 수집기 파이프라인 구동 및 대시보드 실시간 갱신 확인
- [x] 단위 테스트 35개 전수 통과 및 구문 검사 통과
- [x] 백로그 무결성 검증 통과

---

## 📝 작업 기록
- 2026-09-22: 작업 생성 및 `in_progress` 전환 (T-071)
- 2026-09-22: `.github/workflows/vcp-scanner.yml` 파일 삭제
- 2026-09-22: `.github/workflows/collector.yml`을 단일 초경량 5분 상시 실행 워크플로로 재작성
- 2026-09-22: `collector.py`의 `render_markdown_dashboard` 개선 (상단 요약 바, 긴급 매도 권고 표, 돌파모드 컬럼)
- 2026-09-22: 루트 `README.md` 상단 타이틀 및 설명 5분 상시 리프레시로 개편
- 2026-09-22: `collector.py` 실전 구동 (신규 2건 조기돌파 포착 및 대시보드 갱신 반영)
- 2026-09-22: 단위 테스트 35개 전수 통과 및 백로그 `done` 완료 처리


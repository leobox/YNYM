# T-030: 매물대 돌파 + 거래대금 급증 + RSI 과매도반등 병렬 실험 전략 추가

- **상태**: todo (할일)
- **우선순위**: P1
- **카테고리**: quant_ops
- **단계**: 2. 퀀트 데이터 자동 수집
- **예상 소요**: 30분
- **작업 위치**: `TBD`
- **선행 작업**: 없음

---

## 📋 요약
60분봉 기준 최근 N봉 거래량 밀집구간(매물대) 상단을 당일 신규 돌파 + 직전 20봉 대비 거래량 급증 + RSI(14)가 과매도권에서 Signal(6)선을 상향돌파하는 3조건 동시충족 종목을 독립 strategy_version(resistance_breakout_rsi_v1)으로 병렬 전진추적. volume_zscore_accel_v1은 유지, 대체 아님

---

## 🎯 완료 조건 (DoD)
- [ ] 요구사항 구현 및 검증 완료
- [ ] lint / build 확인
- [ ] 문서 최신화

---

## 📝 작업 기록
- 2026-09-18: 작업 생성
- 2026-09-18: 사용자가 공유한 피노(033790) 60분봉 차트(매물대 돌파+거래대금 급증+RSI 과매도반등이
  겹치는 구간에서 +9%대 급등)를 계기로 요청됨. 거래대금 z-score 실험(volume_zscore_accel_v1)을
  대체하지 않고 병행 추적하기로 사용자와 확인.
- 2026-09-18: `collector.py`에 구현
  - `_rsi(close, period)`: 단순 평균 gain/loss 기반 RSI(14) 헬퍼 (`_atr` 옆에 추가)
  - `resistance_breakout_rsi_candidate(df)`: 직전 120봉(현재봉 제외)의 (종가,거래량) 분포를
    15개 구간으로 나눠 최대 거래량 구간(매물대/POC) 상단을 찾고, 이번 봉에 처음 그 상단을
    돌파했는지(`prev_close <= resistance_level < cur_close`) 확인. 돌파봉 거래량이 직전 20봉
    평균 대비 `RESISTANCE_VOL_RATIO_MIN`배 이상, RSI(14)가 최근 10봉 내 과매도권
    (`RESISTANCE_RSI_OVERSOLD`=35 이하)을 찍은 뒤 이번 봉에 Signal(6)선을 상향 돌파해야 통과.
  - `score_stock`에서 매 종목마다 계산 → `run_collector`에 4D 블록으로 등록(strategy_version=
    `resistance_breakout_rsi_v1`), README 대시보드 `experiments` 목록에 세 번째 항목으로 추가.
  - `tests/test_resistance_rsi.py` 신규 작성 (5케이스: 정상 감지, 돌파 없음, 거래량 급증 없음,
    과매도 없음, 봉 부족) — 손으로 계산한 시나리오가 첫 실행에 바로 통과.
- 2026-09-18: 사용자 확인 후 `RESISTANCE_VOL_RATIO_MIN`을 2.0 → 2.5로 조정.
- 완료 근거: 전체 테스트 30건(신규 5건 포함) 통과, 문법 검증 통과. 검증 표본 없는 신규
  가설이므로 운영 신호·다른 실험에 영향 없이 별도 strategy_version으로만 추적 시작.

# T-009: quant_guard 안전검사 범위를 quant-collector로 확장

- **상태**: todo (할일)
- **우선순위**: P0
- **카테고리**: quant_ops
- **단계**: 2. 퀀트 데이터 자동 수집
- **예상 소요**: 30분
- **작업 위치**: `TBD`
- **선행 작업**: 없음

---

## 📋 요약
tools/ai/quant_guard.py의 PROJECT가 quant-research로 하드코딩되어 pre-commit 훅이 quant-collector(exit_engine.py 등 매도/포지션 로직)의 주문 API 금지 검사를 전혀 수행하지 않음. 검사 대상 프로젝트를 다중화하여 quant-collector도 포함시킨다.

---

## 🎯 완료 조건 (DoD)
- [x] `tools/ai/quant_guard.py`: `PROJECT` 단일 문자열 → `PROJECTS = ("quant-research", "quant-collector")` 튜플로 확장, staged/non-staged 스캔 모두 두 폴더 순회
- [x] `tools/ai/lifecycle.cjs`: stop 훅의 quant 검사 게이트를 `quant-research`뿐 아니라 `quant-collector`에서도 동작하도록 확장 (`gym-app` 등 무관 프로젝트는 계속 제외)
- [x] `.githooks/pre-commit`, `docs/ai-setup.md` 설명 갱신 (연구 폴더 단독 표현 제거)
- [x] 회귀 테스트 추가: `test_hooks.py::GuardTests::test_quant_collector_is_also_scanned` (주문 API 삽입 시 non-staged/staged 모두 탐지 확인)
- [x] `python tools/ai/quant_guard.py` 전체 실행 결과 35개 소스 검사 통과 (기존 32개[quant-research] + quant-collector 3개 `.py`)
- [x] `python -m unittest discover -s tools/ai -p "test_*.py"` 11개 테스트 전체 통과

## 📝 원인
`tools/ai/quant_guard.py`와 `tools/ai/lifecycle.cjs`가 quant-collector가 얇았던 시점(T-004 초기)에 quant-research만 스캔하도록 작성됨. 이후 quant-collector에 exit_engine.py(포지션 청산 판정) 등 매도/매수 인접 로직이 추가됐지만 가드 스코프는 갱신되지 않아, 주문 API 금지라는 최상위 안전 불변식이 quant-collector 커밋에는 자동으로 적용되지 않는 사각지대가 있었음.

---

## 📝 작업 기록
- 2026-09-17: 작업 생성
- 2026-09-17: quant_guard.py/lifecycle.cjs 스코프를 quant-collector까지 확장, 회귀 테스트 추가 후 완료 처리 (Claude Sonnet 5)

# T-032: 장마감 안전망 스케줄 추가 (16:50 KST, 재시도 포함)

- **상태**: todo (할일)
- **우선순위**: P1
- **카테고리**: quant_ops
- **단계**: 2. 퀀트 데이터 자동 수집
- **예상 소요**: 30분
- **작업 위치**: `TBD`
- **선행 작업**: 없음

---

## 📋 요약
장중 스케줄(09:37~16:37)이 오늘 전부 실패해도 아침에 최소 1건의 추천은 보장되도록, 16:50 KST에 안전망 실행을 추가. 60분봉은 항상 range=60d로 받아와 장마감 후에도 하루치를 한 번에 수집 가능하므로, 오늘 16:00 이후 성공한 스캔이 없을 때만 실행되도록 check-holiday job에서 판단. 시세 수집 스텝에는 최대 3회 재시도(백오프) 추가

---

## 🎯 완료 조건 (DoD)
- [ ] 요구사항 구현 및 검증 완료
- [ ] lint / build 확인
- [ ] 문서 최신화

---

## 📝 작업 기록
- 2026-09-18: 작업 생성. 사용자 요청: "60분 봉을 한번에 하루치 수집할 수 있으면, 16시 이후에
  실패하면 리트라이해서 돌고 아침에 켰을 때 추천이라도 해줄 수 있으면 좋겠다." — 오늘 장중
  스케줄이 사실상 한 번도 자동으로 안 돈 것을 확인한 뒤([[T-028]] 참고) 나온 요청.
  `fetch_bars`가 이미 항상 `range=60d, interval=60m`으로 받아오므로 장마감 후 1회 실행해도
  그날 완료봉 전체를 놓치지 않고 확보 가능하다는 점을 활용.
- 2026-09-18: `.github/workflows/collector.yml` 수정
  1. `on.schedule`에 `'50 7 * * 1-5'`(16:50 KST) 추가 — 장마감 안전망 슬롯.
  2. `check-holiday` job에 `actions/checkout` 추가하고, 이 슬롯(`github.event.schedule`로 식별)일
     때만 `quant-collector/data/<오늘날짜>/`에 16:00 이후 타임스탬프(`HHMM_scores.csv`)가 이미
     있는지 확인 — 있으면 `should_run=false`로 중복 실행 생략, 없으면 실행.
  3. "시세 수집 스크립트 실행" 스텝에 최대 3회 재시도(30s/60s 백오프) 추가 — 안전망뿐 아니라
     모든 트리거에 공통 적용(범용적으로 유용해서 굳이 안전망 전용으로 분리하지 않음).
- 2026-09-18: 검증
  - `python -c "import yaml; ..."`로 워크플로우 전체 파싱 + on.schedule 3개 확인.
  - 3개 run 스크립트를 실제 파일로 추출해 `bash -n` 문법 검사 통과.
  - check-holiday 스크립트를 실제로 실행해 두 분기 모두 확인: (a) 오늘 16:00 이후 스캔 기록
    없음 → `should_run=true`, (b) 가짜로 16:30 스캔 파일을 만들어두면 → `should_run=false`
    (중복 실행 생략). 테스트로 만든 파일/폴더는 정리함.
- 완료 근거: YAML 파싱, bash 문법검증, 실제 실행 테스트(두 분기) 모두 통과.

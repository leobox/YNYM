# T-029: pending_signals 종목당 활성 신호 중복 등록 방지 (dedup)

- **상태**: todo (할일)
- **우선순위**: P1
- **카테고리**: quant_ops
- **단계**: 2. 퀀트 데이터 자동 수집
- **예상 소요**: 30분
- **작업 위치**: `TBD`
- **선행 작업**: 없음

---

## 📋 요약
동일 전략에서 같은 종목이 연속봉마다 조건을 계속 만족하면 봉마다 새 signal_id로 중복 등록되던 버그 수정(기가비스/휴림로봇 사례). 동일 strategy_version+code로 미해소(is_fully_resolved=False) 신호가 이미 있으면 재등록 skip

---

## 🎯 완료 조건 (DoD)
- [ ] 요구사항 구현 및 검증 완료
- [ ] lint / build 확인
- [ ] 문서 최신화

---

## 📝 작업 기록
- 2026-09-18: 작업 생성
- 2026-09-18: 원인 확인 — `pending_signals.json`에서 (strategy_version, code) 기준 4쌍 중복 발견
  (기가비스 420770, 휴림로봇 090710, 229640, 103590 — 각각 09:00봉/10:00봉에서 별도 signal_id로
  중복 등록). `SignalTracker.register_signal`의 멱등성 체크가 `sig_id`(bar_time 포함) 완전
  일치만 봐서, 같은 종목이 다음 봉에서도 조건을 계속 만족하면 새 sig_id로 다시 등록됐음.
- 2026-09-18: 조치
  1. `tracker.py`의 `register_signal`에 sig_id 완전일치 체크 뒤, 동일 strategy_version+code로
     아직 `is_fully_resolved=False`인 신호가 있으면 skip하는 로직 추가.
  2. `tests/test_tracker.py` 신규 작성 — 같은 봉 재호출 멱등성, 다음 봉 재등록 차단, 전략별
     독립 추적, 해소 후 재등록 허용 4가지 케이스 검증.
  3. 기존 `pending_signals.json`에 이미 쌓여있던 4쌍 중복을 정리 — 각 쌍에서 먼저 등록된
     (registered_at_kst 더 이른) 쪽만 남기고 나중 중복 제거. README 대시보드는 다음 수집
     실행(자동/수동) 시 갱신된 tracker 상태로 자동 재생성됨(수동 편집하지 않음).
- 완료 근거: 신규 dedup 단위 테스트 4건 포함 전체 테스트 25건 통과, 기존
  pending_signals.json 중복 4쌍 정리 완료.

# T-041: 오늘 실험 전략 정리, 운영 전략(algorithm260917_v1) 단일화

- **상태**: todo (할일)
- **우선순위**: P1
- **카테고리**: quant_ops
- **단계**: 2. 퀀트 데이터 자동 수집
- **예상 소요**: 30분
- **작업 위치**: `TBD`
- **선행 작업**: 없음

---

## 📋 요약
오늘 하루 검증한 실험 3종(resistance_breakout_rsi_v1은 이미 T-033에서 폐기, volume_zscore_accel_v1은 헤드라인 h5_t10_s5 원복 후에도 여전히 미검증) 중 남아있던 volume_zscore_accel_v1 실험 코드를 완전히 제거하고 운영 전략(algorithm260917_v1) 단일 구성으로 정리. 대시보드에서 실험 섹션 제거, 관련 상수/함수/등록블록/테스트 삭제

---

## 🎯 완료 조건 (DoD)
- [ ] 요구사항 구현 및 검증 완료
- [ ] lint / build 확인
- [ ] 문서 최신화

---

## 📝 작업 기록
- 2026-09-18: 작업 생성. 사용자 지시: "시드머니 늘어나면 다중포지션은 다시 고민, 우선 mp=1로
  가고... 지금 콜렉터/데이터를 갈아끼우는 건 어떻게 생각함" → 원본 봉 이력 축적([[T-040]])은
  채택, 대신 오늘 검증했지만 결론이 안 난 실험(volume_zscore_accel_v1)은 걷어내고 운영 전략
  단일 구성으로 정리하기로 함. resistance_breakout_rsi_v1은 이미 오늘 T-033에서 폐기됨(중복
  아님, 이번엔 z-score만 대상).
- 2026-09-18: `collector.py`에서 제거: `VOLUME_ZSCORE_*` 상수 5개, `volume_zscore_accel_candidate`
  함수, `score_stock`의 `_volume_zscore` 계산 훅, `run_collector`의 "4C. 거래대금 이상탐지 등록"
  블록, CSV 저장 시 `_volume_zscore` drop 컬럼, `experiments` 리스트 자체(대시보드 실험 섹션
  통째로 제거 — `render_markdown_dashboard` 호출에서 `experiments=` 인자 생략, 함수 자체는
  `Optional` 파라미터라 코드 변경 불필요). 상단에 오늘 실험 4종 전체의 폐기 경위를 요약한 주석
  추가. `tests/test_volume_zscore.py` 삭제.
- 2026-09-18: 전체 테스트 22건 통과(이전 27건에서 z-score 테스트 5건 제거), `collector.py` 1회
  실행으로 대시보드가 "🚨긴급매도", "📊추적중신호", "📈누적전진검증성과" 3개 섹션만 표시하고
  실험 섹션이 완전히 사라진 것을 확인. `pending_signals.json`에 남아있던 활성
  volume_zscore_accel_v1 신호 2건은 삭제하지 않고 그대로 둠(제네릭 `update_with_bars`가 전략
  구분 없이 처리하므로 자연 만료됨, resistance_breakout_rsi_v1 폐기 때와 동일한 처리).
- 완료 근거: volume_zscore_accel_v1 관련 코드 전부 제거 확인(grep으로 재확인, 테스트 파일의
  문자열 리터럴 1건만 남음 — 실제 함수와 무관), 전체 테스트 통과, collector.py 실행으로 대시보드가
  운영전략만 표시함을 확인.

# T-024: 독자 설계 거래대금 이상탐지+3봉 연속가속 전략으로 교체

- **상태**: todo (할일)
- **우선순위**: P1
- **카테고리**: quant_ops
- **단계**: 2. 퀀트 데이터 자동 수집
- **예상 소요**: 30분
- **작업 위치**: `TBD`
- **선행 작업**: 없음

---

## 📋 요약
눌림목(trend_pullback_v1)·모멘텀유동성(momentum_liquidity_v1) 2개 실험 폐기(코드+pending 신호 삭제), 두 실험의 약점(단일봉 노이즈/전종목 동일 절대임계값)을 피하도록 설계한 volume_zscore_accel_v1(종목별 거래대금 z-score+3봉연속상승+과열상한)로 교체

---

## 🎯 완료 조건 (DoD)
- [ ] 요구사항 구현 및 검증 완료
- [ ] lint / build 확인
- [ ] 문서 최신화

---

## 📝 작업 기록
- 2026-09-18: 작업 생성
- 2026-09-18 (Claude Code): 구현 완료.
  - **수정 파일**: `quant-collector/collector.py`(`trend_pullback()`, `momentum_liquidity_candidate()` 및 관련 상수·등록 단계 삭제, `volume_zscore_accel_candidate()` 신규 추가, `experiments` 리스트를 단일 항목으로 교체), `quant-collector/data/pending_signals.json`(`trend_pullback_v1`·`momentum_liquidity_v1` pending 신호 12건 수동 삭제).
  - **설계 근거**: 두 폐기 전략의 구체적 약점을 각각 겨냥해 설계함 — (1) 눌림목은 완료봉 1개만 이동평균에 닿아도 통과(단일봉 노이즈) → 최근 3개 완료봉 연속 종가 상승 요구로 대체. (2) 모멘텀·유동성은 전 종목에 동일한 절대 임계값(거래대금 100억원 등)을 적용해 선별력이 없었음(한 스캔에 12종목 동시 통과) → 종목 자신의 최근 20거래일 거래대금 분포 대비 z-score(적응형 임계값)로 대체. 추가로 SMA20 대비 +15% 초과 확장 종목은 추격매수 방지 차원에서 제외.
  - **검증 명령·결과**: `python -m pytest -q`(16 passed), 합성 데이터로 양성/음성 케이스 단위 검증(정상 매칭/비매칭 확인), 라이브 실행 `python collector.py` — 이번 스캔은 0건 매칭(기존 모멘텀 실험이 12건씩 잡던 것과 달리 훨씬 선별적으로 동작, 의도대로).
  - **남은 한계**: 완전 신규 가설이라 quant-research 소급 검증이 전혀 없고 확정 표본 0건. z-score 임계값(2.0)·과열 상한(15%)·조회기간(20일)은 모두 최초 설계값이라 근거가 약함 — 전진 데이터가 쌓이면 재검토 필요. `trend_pullback`/`momentum_liquidity` 관련 리서치 메모(§9.7 등)는 quant-research 쪽에 그대로 남아있어 향후 재참조 가능.

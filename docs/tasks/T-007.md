# T-007: Phase0 — 이전 코드 복사 및 프로젝트 구조 구축

- **담당**: Gemini (Antigravity)
- **상태**: done (백로그 CLI 기준)
- **착수**: 2026-09-17T13:27 KST

## 범위

1. `D:\DEPO_M\agent\` → `quant-research/scanner/`, `research/`, `tests/` 파일 이전
2. 핵심 모듈 분리: pattern, breakout, candle_cleaner, data_adapter, selector, pipeline, renderer, manifest
3. 백테스트 모듈: signal_generator, virtual_execution, account_simulator, report_generator, walk_forward, bias_reporter
4. 테스트 fixtures, requirements.txt

## 수정 파일

- `quant-research/scanner/*.py` (신규)
- `quant-research/research/*.py` (신규)
- `quant-research/tests/conftest.py` (신규)
- `quant-research/requirements.txt` (신규)

## 가정

- 이전 소스의 점수 공식은 `pattern_colab.py`에 인라인되어 있으므로 그대로 추출
- `strategy.py`의 `hourly_pattern()`과 `pattern_colab.py`의 동일 함수는 ATR 계산만 다름 (indicators.compute_ind vs inline _atr). pattern_colab.py 버전을 기준으로 이전
- 미채택 전략 (trend_pullback, is_entry/check_exit)은 이전하지 않음

## 검증

```bash
python -c "from scanner import pattern, breakout, candle_cleaner; print('OK')"
python -c "from research import signal_generator, virtual_execution, account_simulator; print('OK')"
pytest quant-research/tests/ -v --tb=short
```

## 남은 한계

- 실제 야후/네이버 API 호출 테스트는 이 작업에 포함되지 않음
- 이전 프로젝트의 선택된 `results/` 데이터 복사와 검증은 T-008에서 수행함

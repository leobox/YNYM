# T-010: quant-collector: 청산 신호 엔진(exit_engine.py) 백로그 소급 등록 및 인수인계 문서화

- **상태**: todo (할일)
- **우선순위**: P2
- **카테고리**: quant_ops
- **단계**: 2. 퀀트 데이터 자동 수집
- **예상 소요**: 30분
- **작업 위치**: `quant-collector/exit_engine.py`, `quant-collector/collector.py`
- **선행 작업**: T-004, T-009
- **원 구현 커밋**: `e3b2886` (feat(collector): implement tactical exit signal engine with real-time position monitoring)

---

## 📋 요약
T-004 완료(전진 라벨러/트래커) 이후, backlog 항목 없이 `quant-collector/exit_engine.py`가 추가되어 `collector.py`에 연결되었다. 이 문서는 이미 구현·배포된 기능을 소급으로 백로그·인수인계 기록에 남기는 목적이며 신규 코드 작업은 아니다.

## 🔍 구현 내용 (소급 기술)
- `exit_engine.evaluate_position_exit(position, recent_bars, current_price, now_kst_str)`: `tracker.pending_signals`에 있는 개별 가상 포지션 1건을 평가해 청산 판정 딕셔너리를 반환하는 순수 함수(부수효과 없음, 외부 API 호출 없음).
- 판정 우선순위(코드 상단 주석 및 `collector.py:710` 호출 순서 기준): `HARD_STOP`(-5% 절대 손절) → `TARGET_HIT`(+10% 목표 달성) → `TRAILING_PROFIT`(고점 대비 되돌림 이익 보존) → `BREAKOUT_COLLAPSE`(돌파선 종가 이탈) → `VOLUME_BEAR`(대량거래 음봉) → `STAGNATION`(2거래일 정체) → `CAUTION`/`HOLD`.
- `collector.py:701-714`에서 매 실행마다 `tracker.pending_signals`를 순회하며 네이버 실시간가로 평가하고, 결과를 콘솔 로그(`매도 권고`)와 `render_markdown_dashboard()`를 통해 `README.md` 모바일 대시보드에만 노출한다. **실제 주문/체결 호출은 없음** — 순수 관측·권고 텍스트 산출로, AGENTS.md 최상위 안전 불변식(주문 API 금지)을 준수한다.
- 데이터 소스: `evaluate_position_exit`는 순수 함수라 자체 테스트가 없었음(회귀 테스트 부재 — 후속 과제로 별도 기록 필요 시 새 backlog 항목으로 등록할 것).

## 🎯 완료 조건 (DoD)
- [x] 기존 구현이 AGENTS.md 최상위 안전 불변식(주문 API 절대 금지)을 위반하지 않음을 코드 리딩으로 확인 — `action_type`은 `KEEP/TAKE_PROFIT/CUT_LOSS` 문자열일 뿐 실제 주문 함수 호출 없음
- [x] `tools/ai/quant_guard.py` 정적 검사가 이제 `quant-collector/exit_engine.py`를 포함해 스캔함 (T-009에서 스코프 확장 완료)
- [x] 백로그(T-010)와 본 인수인계 문서로 구현 사실·설계 의도 기록
- [ ] `evaluate_position_exit`에 대한 단위 테스트 부재 — 후속 작업 필요 (별도 backlog 항목으로 분리 권장, 본 작업 범위 밖)

---

## 📝 작업 기록
- 2026-09-17: 작업 생성
- 2026-09-17: 기존 구현(exit_engine.py) 내용 및 안전 준수 여부 검토, 소급 문서화 후 완료 처리. 단위 테스트 부재는 별도 후속 과제로 남김 (Claude Sonnet 5)

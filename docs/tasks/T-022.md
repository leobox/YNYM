# T-022: 눌림목 재상승(trend_pullback) 병렬 전진추적 실험 추가

- **상태**: todo (할일)
- **우선순위**: P1
- **카테고리**: quant_ops
- **단계**: 2. 퀀트 데이터 자동 수집
- **예상 소요**: 30분
- **작업 위치**: `TBD`
- **선행 작업**: 없음

---

## 📋 요약
D:/DEPO_M/agent/strategy.py의 trend_pullback() 공식을 포팅해 quant-collector에서 기존 algorithm260917_v1 신호와 분리된 strategy_version(trend_pullback_v1)으로 병렬 전진추적, README에 별도 실험 섹션으로 표시

---

## 🎯 완료 조건 (DoD)
- [ ] 요구사항 구현 및 검증 완료
- [ ] lint / build 확인
- [ ] 문서 최신화

---

## 📝 작업 기록
- 2026-09-18: 작업 생성
- 2026-09-18 (Claude Code): 구현 완료.
  - **수정 파일**: `quant-collector/collector.py`(`trend_pullback()` 포팅, `score_stock`에 눌림목 스냅샷 계산 추가, 병렬 등록 4C 단계, `_render_signal_rows` 헬퍼로 표 렌더링 분리, `render_markdown_dashboard`에 실험 섹션 추가), `quant-collector/tracker.py`(`get_summary_stats(strategy_version=...)` 필터 추가).
  - **가정**: 원본 공식은 `D:\DEPO_M\agent\strategy.py`의 `trend_pullback(df)`를 그대로 포팅(재구성 아님). ATR은 collector.py 기존 `_atr()` 재사용(DEPO_M의 `compute_ind()['atr']`와 동일 공식: TR 14봉 롤링 평균). exit_engine의 `features["breakout_level"]`을 눌림목 지지선(ma12/ma26)으로 재사용해 별도 청산 로직 없이 기존 HARD_STOP/BREAKOUT_COLLAPSE 판정을 그대로 적용.
  - **부수 수정(버그)**: 기존 마스터 표가 `code` 기준으로 평가 결과를 매칭해서, 같은 종목이 서로 다른 시각/전략으로 2개 이상 pending 신호를 가질 때 평가 결과가 뒤섞이는 문제가 있었음(`eval_by_code` 충돌). `signal_id` 기준 매칭(`eval_by_sigid`)으로 수정.
  - **검증 명령·결과**: `python -m pytest -q`(16 passed), `node tools/ai/check_syntax.cjs`(통과), 실제 네트워크 접속 라이브 실행 `python collector.py` 3회 — 눌림목 후보 1건(상신이디피) 정상 등록·평가·README 실험 섹션 표시 확인, 루트 README embed에서는 실험 섹션 미노출 확인.
  - **남은 한계**: 눌림목 전략은 quant-research 9.7절 소급 백테스트에서 이미 "기간별·지연별 안정성 없음"으로 미채택 결론이 난 공식이라, 이번 병렬 추적도 실제로 운영 신호보다 나은지는 전혀 검증되지 않은 상태(확정 표본 0건). 채택 여부는 전진 검증 표본이 충분히 쌓인 뒤 별도로 판단해야 함. `strategy_version`이 다른 동일 종목 중복 표시 문제(코드 중복)는 이번 범위 밖으로 남겨둠.

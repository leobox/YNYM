# T-067: quant-collector: GitHub Actions 스케줄 5분화 및 5분·10분·30분·60분 다중 분봉 수집·재검증 체계 구축

- **상태**: done (완료)
- **우선순위**: P1
- **카테고리**: quant_ops
- **단계**: 2. 퀀트 데이터 자동 수집
- **예상 소요**: 45분
- **작업 위치**: `.github/workflows/`, `quant-collector/collector.py`, `quant-collector/revalidate.py`, `quant-collector/tests/test_bar_history.py`, `README.md`
- **선행 작업**: T-066

---

## 📋 요약
1. **GitHub Actions 스케줄 5분화 및 기존 스케줄 삭제**:
   - `vcp-scanner.yml`의 기존 16:30, 18:00 cron 스케줄 삭제 (수동 `workflow_dispatch`만 유지).
   - `collector.yml`의 기존 30분 주기(7,37분) 및 16:50 장마감 안전망 스케줄을 삭제하고, 한국 정규장 5분 주기(`cron: '*/5 0-6 * * 1-5'`, 09:00~15:55 KST, 월~금)로 전환.
2. **다중 분봉(5m/10m/30m/60m) 수집 및 이력 보관 구조 확장**:
   - 기존 60분봉은 경로(`data/bars_history/{code}.csv`) 및 인터페이스 하위 호환성 100% 유지.
   - 단 1회의 5m API 요청으로 5m, 10m, 30m, 60m 완료봉을 정합성 있게 일괄 추출(`parse_multiframe_bars`, `fetch_all_bars`).
   - 종목별 분봉 데이터 증분 저장 지원: `{code}_30m.csv`, `{code}_10m.csv`, `{code}_5m.csv`.
   - 5분봉 완료봉 캔들(최근 400개, 5거래일 커버)을 `SignalTracker` 및 `exit_engine`에 전달하여 5분 단위 초정밀 접촉 및 청산 판정 체계 구축.
3. **기존 포착 신호(24건) 대상 다중 분봉 전진 추적 재검증 수행**:
   - 재검증 스크립트(`quant-collector/revalidate.py`) 구현 및 리포트(`quant-collector/data/multiframe_revalidation_report.md`) 도출.
   - 60분봉에서 뭉개지던 장중 손절/익절 선접촉 선후관계와 조기 청산 도달 시점을 5분 단위로 완벽하게 규명.
4. **품질 및 안전성 검증**:
   - 주문/거래 로직 0건, 타임아웃 10초, 지수 백오프, 멱등성 보장(Collector Safety Check 통과).
   - 단위 테스트 35개 전수 통과.
   - 유니버스 350종목 다중 분봉 실전 수집 성공 350 / 에러 0 달성.

---

## 🎯 완료 조건 (DoD)
- [x] `vcp-scanner.yml` 스케줄 제거 및 `collector.yml` 한국 장중 5분 스케줄(`*/5 0-6 * * 1-5`) 전환
- [x] `parse_multiframe_bars`, `fetch_all_bars`, `save_bar_history` 다중 분봉 지원 구현
- [x] `test_bar_history.py` 다중 분봉 저장 및 리샘플링 단위 테스트 추가 및 35개 전수 통과
- [x] 기존 24개 신호 대상 다중 분봉 재검증 스크립트(`revalidate.py`) 실행 및 리포트 도출
- [x] 350종목 실전 수집 파이프라인 검증 (성공 350 / 에러 0) 및 모바일 대시보드 갱신
- [x] 백로그 무결성 검증 통과

---

## 📝 작업 기록
- 2026-09-22: 작업 생성 및 `in_progress` 전환 (T-067)
- 2026-09-22: `.github/workflows/vcp-scanner.yml`에서 `schedule:` 블록 완전 제거
- 2026-09-22: `.github/workflows/collector.yml`에 `cron: '*/5 0-6 * * 1-5'` 5분 스케줄 반영 및 레거시 30m sleep 제거
- 2026-09-22: `collector.py`에 `parse_multiframe_bars`, `fetch_all_bars`, `save_bar_history` 다중 분봉(5m/10m/30m/60m) 로직 구현
- 2026-09-22: `test_bar_history.py`에 다중 분봉 저장 및 파싱 단위 테스트 2건 추가 (총 35개 테스트 전수 통과)
- 2026-09-22: `revalidate.py` 개발 및 기존 24개 신호 대상 다중 분봉(60m vs 30m vs 10m vs 5m) 비교 검증 완료
- 2026-09-22: `parse_multiframe_bars` 장마감 더미 봉(Volume=0 & High=Low) 필터링 패치 적용
- 2026-09-22: 350종목 실전 수집 실행 (성공 350 / 제외·오류 0), 미해소 신호 6건 선접촉 라벨 확정
- 2026-09-22: 백로그 무결성 검사 통과 및 작업 완료 처리


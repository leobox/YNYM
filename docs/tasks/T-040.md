# T-040: 원본 60분봉 이력 축적 저장 추가 (향후 ML/DL 대비)

- **상태**: todo (할일)
- **우선순위**: P1
- **카테고리**: quant_ops
- **단계**: 2. 퀀트 데이터 자동 수집
- **예상 소요**: 30분
- **작업 위치**: `TBD`
- **선행 작업**: 없음

---

## 📋 요약
매 스캔마다 최종 계산 결과(scores/top5/watch)만 저장하고 원본 60분봉은 버려지는 구조라 향후 피처 재실험이 불가능했음. 종목별 누적 CSV(data/bars_history/{code}.csv)에 그날 새로 완료된 봉만 타임스탬프 기준 중복없이 append하는 방식으로 원본 이력을 별도 축적. 60일 전체를 매번 다시 저장하면 저장소가 과도하게 커지므로 신규분만 증분 저장

---

## 🎯 완료 조건 (DoD)
- [ ] 요구사항 구현 및 검증 완료
- [ ] lint / build 확인
- [ ] 문서 최신화

---

## 📝 작업 기록
- 2026-09-18: 작업 생성. 배경: 오늘 quant-research 쪽에서 walk_forward 정식 검증을 하면서
  원본 60분봉이 없으면 나중에 새 피처를 소급 실험할 수 없다는 게 계속 걸림돌이었음(quant-research는
  bars_CODE.csv를 이미 이런 용도로 쌓아왔음 — 같은 패턴을 quant-collector에도 적용).
- 2026-09-18: `save_bar_history(code, df_bars)` 구현 — `data/bars_history/{code}.csv`에 종목별로
  누적. 파일이 없으면 fetch_bars가 가져온 60일 전체를 최초 1회 저장, 이후로는 기존 파일의 마지막
  타임스탬프보다 뒤에 있는 신규 완료봉만 append(`df_bars[df_bars.index > last_ts]`). 손상되었거나
  읽기 실패한 파일은 통째로 다시 씀. `run_collector`의 종목별 스캔 루프에서 매 스캔마다 호출.
- 2026-09-18: `tests/test_bar_history.py` 4건 작성(최초 생성, 재실행 시 신규분만 append, 신규
  없으면 no-op, 종목별 파일 분리) — 전부 통과. `collector.py` 1회 실행해 352개 종목 전부
  `data/bars_history/{code}.csv` 생성 확인, 포맷이 quant-research의 bars_CODE.csv와 동일함을
  확인(인덱스=타임존 포함 타임스탬프, Open/High/Low/Close/Volume).
- 완료 근거: 증분 append 로직 구현+테스트 4건 통과, collector.py 실행으로 352개 종목 정상 축적
  확인(재실행 시 new_rows 없으면 파일 크기 불변도 테스트로 검증됨).

# T-025: 유니버스를 KOSPI200+KOSDAQ150 시총 근사로 교체

- **상태**: todo (할일)
- **우선순위**: P1
- **카테고리**: quant_ops
- **단계**: 2. 퀀트 데이터 자동 수집
- **예상 소요**: 30분
- **작업 위치**: `TBD`
- **선행 작업**: 없음

---

## 📋 요약
시가총액 1천억~5조 밴드+거래대금 상위150 방식을 KOSPI 시총상위200+KOSDAQ 시총상위150(약 350종목, 공식 지수 근사)으로 교체. KRX 공식 지수 API는 세션 인증 필요로 접근 불가해 네이버 시총랭킹으로 근사. quant-research 설계문서 §4.1도 동기화

---

## 🎯 완료 조건 (DoD)
- [ ] 요구사항 구현 및 검증 완료
- [ ] lint / build 확인
- [ ] 문서 최신화

---

## 📝 작업 기록
- 2026-09-18: 작업 생성
- 2026-09-18 (Claude Code): 구현 완료.
  - **수정 파일**: `quant-collector/collector.py`(`get_universe()`를 시장별 개별 top-N 방식으로 재작성, `MCAP_MIN`/`MCAP_MAX`/`SCAN_LIMIT` 제거, `KOSPI_TOP_N=200`/`KOSDAQ_TOP_N=150` 추가), `quant-research/설계문서.md` §4.1(새 유니버스 기준 반영 + 기존 "코드 0으로 끝나는 종목 제외" 오탈자 수정: 실제로는 0으로 끝나지 않는 종목이 제외됨).
  - **시도했으나 실패한 방법**: FinanceDataReader(`KOSPI200`/`KOSDAQ150` 미구현), pykrx(`get_index_portfolio_deposit_file` 등 — KRX 공식 API가 세션/로그인 필요로 이 환경에서 빈 응답), 네이버 모바일 API의 지수 구성종목 엔드포인트 추정(`/api/index/KPI200/{components,constituents,stocks,...}` 전부 404), KRX 데이터포털 직접 POST(세션 없이 파라미터만 흉내내자 `LOGOUT` 응답). 사용자와 상의 후 시가총액 랭킹 기반 근사로 결정.
  - **가정**: KOSPI 시총 상위 200 + KOSDAQ 시총 상위 150을 공식 KOSPI200/KOSDAQ150 지수의 근사치로 사용한다. 공식 지수는 시총 외에 유동성 스크리닝 등 추가 기준이 있어 완전히 일치하지 않는다. 우선주 제외(`code.endswith("0")`)·스팩/리츠 제외·2,000원 가격 하한은 기존 로직 유지.
  - **검증 명령·결과**: `python -m pytest -q`(16 passed), `node tools/ai/check_syntax.cjs`(통과), `get_universe()` 단독 실행(3초, KOSPI 200 + KOSDAQ 149건, 최상위 종목 삼성전자 확인 — 이전에는 5조 상한 때문에 제외됐던 대형주가 정상 포함됨), 라이브 전체 실행 `python collector.py`(352종목 78초 소요, GitHub Actions 30분 주기 내 충분히 여유).
  - **남은 한계**: 공식 지수 구성종목과 정확히 일치하지 않음(유동성 등 KRX의 추가 스크리닝 기준 미반영). 스캔 종목 수가 150→352로 약 2.3배 늘어 신호 빈도·특성이 이전과 달라질 수 있음(대형주 포함으로 인한 변동성 감소 등, 향후 전진검증으로 확인 필요). KRX 공식 API 접근은 여전히 막혀 있어, 필요 시 KRX_ID/KRX_PW 계정 발급 후 pykrx 재시도가 대안으로 남아있음.
